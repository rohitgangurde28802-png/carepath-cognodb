{
  "name": "carepath-cognodb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Explainable specialist referrals powered by CognoDB",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "seed": "node scripts/seed.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/graph/repository.js && node --check scripts/seed.js && node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "neo4j-driver": "^5.28.1"
  }
}
